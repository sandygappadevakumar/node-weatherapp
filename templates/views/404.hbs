<!DOCTYPE html>
<html>
    <header>
        <link rel="stylesheet" href="css/styles.css">
    </header>
    <body>
        <div class="main-content">
            {{>header}}
            <p>{{errorMsg}}</p>
        </div>
        {{>footer}}
    </body>
</html>